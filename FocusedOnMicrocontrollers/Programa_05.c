/*Programa que ejecuta la operacion or entre dos variables*/

#include<stdio.h>

int main(){
  int a=3;
  int b=5;
  int resultado=0;
  printf("valor de a y b en hexadecimal %X, %X\n",a,b);
  //resultado=a && b;    //operacion and entre a y b
  printf("Despues de la operacion and: %X\n",resultado);

  return 0;
}
