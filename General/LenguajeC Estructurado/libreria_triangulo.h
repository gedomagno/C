#include<stdio.h>

float t_area(float base,float altura){
    return((base*altura)/2);
                       }

float t_perimetro(float base){
    return(base*3);
                       }
                       
